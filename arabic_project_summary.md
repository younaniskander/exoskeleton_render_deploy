# نظام التنبؤ بحركة الهيكل العظمي الخارجي باستخدام بيانات مستشعرات العضلات الحيوية (EMG)

هذا المشروع عبارة عن نظام متكامل فائق الأداء يعتمد على **معالجة الإشارات الرقمية (DSP)** و **التعلم الآلي (Machine Learning)** للتنبؤ بزاوية ثني مفصل الركبة (`knee_angle_r`) لمستخدم الهيكل العظمي الخارجي (Exoskeleton) في الوقت الفعلي.

من خلال قراءة الإشارات الكهربائية الحيوية للعضلات (Electromyography - EMG)، يستطيع الهيكل العظمي الخارجي التنبؤ بحركة المستخدم ومزامنة سرعته وزاويته لتوفير مشي طبيعي وسلس.

---

## 📐 الهيكل العام للنظام (System Architecture)

يتم تشغيل النظام وفق الخطوات التالية:

1. **مستشعرات العضلات (EMG Sensors)**: تلتقط النشاط الكهربائي من 11 عضلة في الجزء السفلي من الجسم بتردد 1000 هرتز.
2. **خط معالجة الإشارات (DSP Pipeline)**: ينظف الإشارات الحيوية من التشويش ويحولها إلى غلاف خطي ناعم، ثم يقلل التردد إلى 100 هرتز ليتوافق مع مستشعرات الحركة.
3. **استخراج الخصائص (Feature Extraction)**: تقسيم الإشارات إلى نوافذ زمنية متداخلة واستخراج 3 خصائص رئيسية لكل نافذة (بمجموع 33 خاصية).
4. **نموذج التعلم الآلي المدرب (PyCaret Regressor)**: يستقبل الخصائص الـ 33 ويتنبأ بزاوية الركبة بالدرجات فوراً.
5. **خادم FastAPI**: يوفر واجهة برمجة تطبيقات (API) عامة لتشغيل هذا النموذج في الوقت الفعلي بسرعة فائقة لا تتعدى 2 مللي ثانية.

---

## ⚡ خط معالجة الإشارات الرقمية بالتفصيل (`pipeline.py`)

إشارات العضلات بطبيعتها تحتوي على ضوضاء عالية نتيجة حركة الأسلاك والأقطاب. يمرر الكود الإشارة عبر المراحل التالية للتنظيف:

1. **مرشح التمرير العالي (High-pass Filter)**: مرشح Butterworth من الدرجة الرابعة بقطع عند **20 هرتز** للتخلص من ضوضاء الحركة الأساسية.
2. **التقويم (Rectification)**: تحويل جميع قيم الإشارة المتذبذبة إلى قيم مطلقة موجبة ($|EMG|$) لمعرفة شدة الانقباض.
3. **مرشح الغلاف الخطي (Low-pass Envelope Filter)**: مرشح Butterworth لتمرير الترددات المنخفضة تحت **6 هرتز** للحصول على شكل انسيابي يمثل المجهود الفعلي للعضلة.
4. **تقليل عينات الإشارة (Decimation)**: خفض التردد من 1000 هرتز إلى 100 هرتز باستخدام خوارزمية Decimate من SciPy لضمان المزامنة مع بيانات الحركة الحركية (Kinematics).
5. **التقطيع الزمني المتداخل (Sliding Windowing)**: تقطيع البيانات إلى نوافذ بطول **200 مللي ثانية (20 عينة)** وبنسبة تداخل **50%** للحفاظ على استمرارية القراءة وتفادي التأخير.
6. **استخراج الخصائص الرياضية (Feature Extraction)**: لكل نافذة، يتم حساب 3 مؤشرات رياضية لكل قناة عضلية:
   * **القيمة المطلقة المتوسطة (MAV)**: تعبر عن قوة نشاط العضلة الإجمالي.
   * **جذر متوسط المربعات (RMS)**: يعبر عن الطاقة والجهد الكهربائي الفعلي للعضلة.
   * **طول الموجة (WL)**: يعبر عن سرعة تقلبات الإشارة وتغيراتها الترددية.

---

## 🧬 قنوات استشعار العضلات الـ 11 ومواقعها الحيوية

يقرأ النظام من 11 مستشعراً للعضلات موزعة كالتالي:

| اسم القناة في الكود | اسم العضلة بالعربية | دورها الحيوي أثناء المشي |
| :--- | :--- | :--- |
| `gastrocmed` | العضلة التوأمية الساقية | تدفع الجسم للأمام أثناء مرحلة الدفع (Push-off). |
| `tibialisanterior` | العضلة الظنبوبية الأمامية | ترفع مشط القدم لتجنب التعثر أثناء مرحلة التأرجح. |
| `soleus` | العضلة النعلية | تدعم وتثبت الساق أثناء الوقوف والمشي. |
| `vastusmedialis` | العضلة المتسعة الإنسية | تساهم في بسط وتثبيت مفصل الركبة. |
| `vastuslateralis` | العضلة المتسعة الوحشية | المحرك الرئيسي لبسط الركبة عند ملامسة الأرض. |
| `rectusfemoris` | العضلة الفخذية المستقيمة | تساهم في ثني الفخذ وبسط الركبة معاً. |
| `bicepsfemoris` | العضلة ذات الرأسين الفخذية | تثني الركبة وتتحكم في فرملة الساق عند نهاية الخطوة. |
| `semitendinosus` | العضلة نصف الوترية | تعمل مع عضلات المأبض لثني الركبة ومد الفخذ. |
| `gracilis` | العضلة الرشيقة | تساعد في تقريب الفخذ وتساهم في ثني الركبة. |
| `gluteusmedius` | العضلة الألوية الوسطى | تحافظ على توازن الحوض وتمنع هبوطه عند رفع القدم الأخرى. |
| `rightexternaloblique` | العضلة المائلة الخارجية | تدعم استقرار الجذع والبطن لمنع الانحراف الجانبي. |

*إجمالي الخصائص المدخلة للنموذج:* $11 \text{ قنوات} \times 3 \text{ خصائص} = 33 \text{ خاصية}$.

---

## 🤖 تدريب واختيار النموذج الذكي (`train.py` & `run_pipeline_viz.py`)

يعتمد التدريب على قاعدة بيانات **Camargo** الشهيرة للمشي البشري.
1. **تجهيز البيانات**: تتم مزامنة إشارات العضلات مع زاوية الركبة اليمنى المستخرجة من التحليل الحركي العكسي (IK).
2. **استخدام مكتبة PyCaret**:
   * تقوم المكتبة بعمل معالجة تلقائية للبيانات (Normalization) وتحسين توزيعها (Power Transformation).
   * إزالة التداخل الخطي بين القنوات (Multi-collinearity removal) لمنع التكرار وحماية النموذج من التشتت.
   * مقارنة النماذج المختلفة واختيار الأفضل تلقائياً بناءً على أقل نسبة خطأ (RMSE).
3. **النماذج المجرّبة**: Ridge Regression، و Random Forest، و Gradient Boosting.
   * تفوق نموذجا **Gradient Boosting** و **Random Forest** في التقاط العلاقات المعقدة وغير الخطية بين جهد العضلات وزاوية الحركة الفخرية للركبة.

---

## 🌐 خادم تشغيل النموذج الحقيقي كـ API عام (`app.py`)

تم توفير خادم ويب فائق السرعة باستخدام إطار عمل **FastAPI** لتشغيل النموذج الحقيقي للذكاء الاصطناعي:

### ⚙️ وظائف خادم الويب:
* **تحميل النموذج الفعلي**: يقوم السيرفر بتحميل ملف النموذج المعتمد `best_regressor_model.pkl` الذي تم إنتاجه بواسطة PyCaret مباشرة.
* **نقطة الفحص الصحي (`/health`)**: تعرض حالة النظام ومدى توفر وجاهزية النموذج للعمل.
* **نقطة التنبؤ الفوري (`/predict`)**:
  * تستقبل نافذة إشارات عضلات خام (`emg_window`) من العميل بصيغة JSON.
  * تطبق خط المعالجة الرياضي (DSP) بالكامل في أجزاء من الثانية.
  * تنفذ التنبؤ باستخدام خوارزمية PyCaret وتوفر زاوية الركبة الدقيقة والزمن المستغرق للتنبؤ.

---

## 🚀 خطوات تشغيل ونشر الـ API ليكون عاماً (Public API) بدون مساعدة

تم إعداد ملف `app.py` ليرتبط بجميع المنافذ ويقبل الاتصالات الخارجية (`0.0.0.0`). لجعل السيرفر عاماً ومتاحاً لأي شخص عبر الإنترنت، اتبع إحدى الطريقتين التاليتين:

### **الطريقة الأولى: استخدام Ngrok (للتشغيل المباشر من جهازك الشخصي)**
1. قم بفتح سطر الأوامر (Terminal) في مسار المشروع وتشغيل السيرفر:
   ```powershell
   python exoskeleton_pipeline/app.py
   ```
2. افتح سطر أوامر جديد وقم بتشغيل أداة **ngrok** لربط خادمك بالإنترنت مجاناً:
   ```powershell
   ngrok http 8000
   ```
3. ستعطيك الأداة رابطاً عاماً مشفراً مثل: `https://xxxx-xxxx-xxxx.ngrok-free.app`
4. يمكنك استخدام هذا الرابط لإرسال الطلبات مباشرة من أي هاتف أو كمبيوتر في العالم إلى جهازك الشخصي للتنبؤ بحركة الركبة!

### **الطريقة الثانية: النشر على سيرفر سحابي (Render)**
1. ارفع الكود بالكامل إلى حسابك في **GitHub**.
2. توجه إلى موقع **Render.com** وقم بإنشاء **Web Service** جديدة وربطها بالمستودع الخاص بك.
3. اضبط الإعدادات التالية:
   * **Start Command**: `python exoskeleton_pipeline/app.py`
   * **Build Command**: `pip install -r requirements.txt`
4. سيوفر لك الموقع رابطاً سحابياً عاماً ودائماً يعمل على مدار الساعة لخدمة الهيكل العظمي الخارجي!
